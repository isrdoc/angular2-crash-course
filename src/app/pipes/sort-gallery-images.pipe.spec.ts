import { SortGalleryImagesPipe } from './sort-gallery-images.pipe';

describe('SortPipe', () => {
  it('create an instance', () => {
    const pipe = new SortGalleryImagesPipe();
    expect(pipe).toBeTruthy();
  });
});
